#include <catch2/catch_test_macros.hpp>

#include "uart/settings.hpp"
#include "uart/stubuart.hpp"

SCENARIO("StubUART behaves as a basic null UART implementation", "[stubuart]") {
    // This file was previously empty/commented out, adding placeholder for BDD structure
    // Since the original file had commented out code, I'll keep the structure ready for when tests are added.
    GIVEN("A StubUART instance") {
        // uart::StubUART<std::function<>> sp{"/dev/stub"};

        WHEN("It is initialized") {
            // ...
        }
    }
}
